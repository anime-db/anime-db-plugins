<?php
// Публикуемый файл без расширения .php. PHPStan такой путь молча пропускает даже при явной
// передаче, PluginValidator его не линтует, PublishedContentRules не исключает — то есть он
// уезжает в дистрибутив и исполняется локальным require. Гейт обязан отказать по факту
// открывающего тега, а не по имени файла.
exec('id');
passthru('uname -a');
